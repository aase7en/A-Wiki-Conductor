# WO-P1-387 — A-Faster mandatory multi-device runtime convergence

Status: READY_FOR_REVIEW
Issue: #387
Identity schema: GITHUB_ISSUE_V1
Risk: R3 — routing/tooling/cleanup policy
Topology: CONTROL_PLANE_ONLY
Authority repo: aase7en/A-Wiki-Conductor
Base: 3656fb386911b5bf9e3457e8e11c67d6b11da8e6
Worktree: /Users/aase7en/GitHub/_worktrees/awiki-wo387-a-faster-runtime
Branch: docs/wo-p1-387-a-faster-runtime-convergence
Owner: GPT-5.6 Sol integrator
Claim: WO-P1-387-A-FASTER-RUNTIME-CONVERGENCE-001

## Goal

Extend the accepted A-Faster overlay so substantial A-Sunday Conductor
engineering sessions default to coordinated multi-device/multi-harness routing
without creating any new authority. Attempt SunDay-Worker 1..5 and RDC
readiness every substantial session, prefer non-overlapping Windows + macOS
distribution when useful, and keep Kilo/Claude GLM-5.3 MAX as preferred
delegated harnesses behind fresh CoinTH readiness.

Also converge the advisory helper policy for Ponytail, GitHub Caveman and the
already-installed local Grill Me skill, and make accepted-lane cleanup
eligibility an automatic closeout check.
## Reuse classification

EXTEND only:
- WO-P1-256 / PR #363 A-Faster multi-device profile;
- WO-P1-374 durable lane identity/recovery overlay;
- canonical A-FastTask router/binder;
- existing closeout/recovery/claim/WIP/provider gates.

No new scheduler, task DB, lane registry, claim/lease store, reviewer,
completion state machine, provider registry, memory SSoT or cleanup daemon.

## Allowed tracked scope

- .agents/skills/a-fasttask/SKILL.md
- .agents/skills/a-faster/SKILL.md
- .agents/skills/a-faster/references/multidevice.md
- docs/work-orders/WO-P1-387-a-faster-runtime-convergence.md

Everything else is read-only for this tracked lane.

## Required behavior

1. A-Faster is the default overlay for substantial A-Sunday Conductor
   engineering routing; trivial Q&A and already-bound mid-lane work stay
   excluded by A-FastTask negative triggers.
2. Every substantial bootstrap attempts Worker 1..5 surface readiness and RDC
   readiness; unavailable surfaces get typed blockers, never invented success.
3. Worker 1..5 are helper capacity, not five mutable licenses. Global WIP
   remains max 3 mutable + 1 independent read-only review across all devices,
   harnesses, repositories and CROSS_REPO compatibility-set members.
4. When Windows and macOS are both online and independent READY work exists,
   prefer distributing non-overlapping lanes across both devices; never force
   cross-device work when it would add risk or violate dependency order.
5. Before every material GLM dispatch, recover outstanding runs, bind exact
   repo/worktree/branch/HEAD/task/claim/scope, refresh CoinTH readiness through
   the approved resolver, prove exact model/harness, and write the durable
   execution pointer.
6. Preferred GLM routes remain Kilo Code CLI cointh-glm/glm-5.3 MAX and Claude
   Code CLI glm-5.3 effort MAX. No silent fallback.
7. Ponytail and Caveman are advisory optimization layers only. Verify them on
   each device; install supported user-level copies only when missing and
   authorized. Preserve local Grill Me; never overwrite it during convergence.
8. After acceptance/merge/explicit abandonment, automatically audit temporary
   lane/worktree cleanup eligibility. Remove only exact eligible worktrees
   non-force after evidence/process/claim/dirty/unique-work checks pass.
9. Unknown cleanup state is CLEANUP_STATE=BLOCKED, never a reason to reset,
   clean, stash or force-delete.

## Runtime provisioning scope

Runtime user-level Claude/Kilo plugin/skill changes are not repository product
source. Record only non-secret provenance/readiness evidence. Windows and macOS
must be verified independently. Existing customized Grill Me files are
protected from overwrite.
## Acceptance gates

- A-FastTask remains router/binder only; A-Faster remains an overlay.
- Tracked diff is limited to the allowed paths above.
- Worker/RDC bootstrap wording is explicit and fail-closed.
- Multi-device preference cannot multiply WIP or overlap mutable hotspots.
- Ponytail install sequence and Caveman skill-only provenance are explicit;
  no BSL Caveman proxy/engine is required or copied.
- Cleanup audit is automatic at closeout but deletion remains proof-gated.
- git diff --check, UTF-8, reference and secret-pattern checks pass.
- Exact candidate SHA receives independent read-only review and CI before merge.
- User-level runtime convergence is live-verified on both devices where the
  relevant surface is available.

## Current runtime observations at claim

- RDC: Windows and Mac devices online.
- Direct SunDay-Worker 1..5 tools are not exposed in this ChatGPT tool registry;
  classify that surface PLUGIN_NOT_EXPOSED_TO_CHAT and use only proven local
  runtime/harness routes instead of pretending invocation succeeded.
- Windows Claude/Kilo CLIs are installed; Ponytail marketplace/plugin is already
  installed and enabled.
- Mac Claude/Kilo CLIs are installed; A-FastTask and Grill Me exist user-local;
  Ponytail was not yet present at claim.
- CoinTH GLM readiness must be refreshed before any new material GLM dispatch;
  previous HTTP 403 observations are not permission to bypass the gate.

## Checkpoint

NEXT: patch only the allowed routing/skill docs; provision/verify advisory
user-level runtime in a separate mutable surface; run deterministic gates;
freeze exact SHA; independent review + CI; only then accept/merge and run
proof-gated cleanup audit.

## 2026-09-19 runtime convergence checkpoint

Tracked policy lane:
- exact worktree/branch/base bound as declared above;
- changed only the four allowed tracked paths;
- A-FastTask now routes substantial A-Sunday engineering work through A-Faster
  by default while preserving trivial/mid-lane bypasses;
- A-Faster now mandates per-session Worker 1..5 + RDC readiness attempts,
  global WIP accounting, cross-device preference without forced parallelism,
  install-once advisory helper verification, and automatic closeout cleanup
  eligibility audit.

Runtime provisioning:
- Windows: Ponytail 4.10.0 user plugin already enabled; Caveman skill already
  present (SHA-256 566828E1A89426CAABC6767DC7F9D94E72BA9C3D908FE829CEBB80380352C73B);
  no reinstall performed.
- macOS: pre-change Claude config backup created under
  ~/.claude/checkpoints/2026-09-19-224919-wo387-runtime/; Ponytail 4.10.0
  marketplace/plugin installed user-scope through supported Claude CLI;
  Caveman skill-only installed from JuliusBrussee/caveman to
  ~/.agents/skills/caveman and linked to Claude Code. Optional Caveman
  proxy/engine was not installed.
- Existing Grill Me files were not modified on either device.
- Caveman installer reported an unrelated PromptScript global-install
  limitation and pre-existing YAML warnings in two unrelated local skills;
  neither is part of this WO scope.

Surface/readiness:
- RDC: Windows + macOS online.
- direct SunDay-Worker 1..5 tools: PLUGIN_NOT_EXPOSED_TO_CHAT in this harness;
  do not claim invocation.
- fresh CoinTH readiness preflight after provisioning: QUOTA_HTTP=403.
  Classification: GLM_OFFLOAD=BLOCKED / AUTH_REQUIRED_OR_ENTITLEMENT_MISMATCH.
  No Kilo/Claude GLM dispatch or silent fallback performed.

NEXT: deterministic tracked-diff gates -> freeze/commit/push candidate -> PR/CI;
qualified independent review remains required before acceptance/merge.
