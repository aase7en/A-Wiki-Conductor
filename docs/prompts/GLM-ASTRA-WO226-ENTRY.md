# Astra -> ZCode GLM-5.3: released WO226 long shift

/goal Execute Sol's released WO226 to one verified candidate or a precise DESIGN_GAP handback.
Use one receiving session with sequential nested subgoals. Continue safe work without asking the
human to type continue or copy results. This entry transports the existing packet; it is not
another MASTER, scheduler, source release, or permission to merge.

## 1. Read the actual contract first

Verified Windows packet worktree:
`A:/GitHub/_worktrees/A-Wiki-Conductor-wo226-zra2-reviewer-execution`

Read there:
- `docs/prompts/GLM-WO226-ZRA2-REVIEWER-EXECUTION-BRIDGE.md`
- `docs/work-orders/WO-P1-226-zra2-reviewer-execution-bridge.md`

Release packet Git commit: `cb7959edce0058603b7b1d3c389fce66f1043007` (PR308).
Release authority: https://github.com/aase7en/A-Wiki-Conductor/issues/214#issuecomment-5647113609
Audit already folded: `f99cd9aadceb903649cdbf577935267a362540eb`.
Accepted main at entry preparation: `251df211afc1ee5452f3652675d7a2f38c526876`.
These are pins, not a substitute for fresh authority/ownership checks.

If a relative document is missing in a fresh source worktree, read the exact packet via
`git show cb7959edce0058603b7b1d3c389fce66f1043007:docs/work-orders/WO-P1-226-zra2-reviewer-execution-bridge.md`
and the corresponding prompt path from the same object. Do not edit the docs-preparation
worktree. A verified immutable copy may live under the source lane's ignored runs/WO-P1-226/
for reference; do not use a stale pointer as current release authority.

Re-pin Issue214 and PR308. If Sol has issued a newer packet, follow only the exact replacement
SHA linked by Sol's durable release/repair decision; do not silently trust a moved branch tip.
A revoke/HOLD supersedes this entry. Never execute obsolete C0/Q2 from an old MASTER checkpoint.

## 2. Ownership and execution location

Use the universal entry sequence from `00-AGENT-ENTRY.md` and the relevant project graph nodes;
read CURRENT-WORK, the active WO and handoff as required. Read DEFECT_LESSONS before source edits.

This file's worktree `A:/GitHub/_worktrees/A-Wiki-Conductor-wo226-astra-dispatch` is delivery-only.
The packet worktree above is Sol-owned. Neither is your source lane.

After fresh fetch/overlap checks, follow WO226's requirement to create a clean isolated source
worktree from then-current accepted main and publish your real session/owner/source claim BEFORE
any source/test change. Record repo, branch, full base/head, exact paths, forbidden paths and result
location in Issue214. Do not infer claim from an installed ZCode process or branch name.

If the same source lane is already owned by your verified previous invocation, resume its exact
checkpoint; if another invocation owns it, preserve work and stop BLOCKED_EXTERNAL_OWNER.
Do not start another GLM goal on top of an active goal. Nested goals here are sequential logical
milestones, not concurrent writers or repeated top-level /goal invocations that replace context.

## 3. Division of responsibility

| Owner | Work |
| --- | --- |
| GLM-5.3 / this one ZCode session | Source archaeology, dependency tracing, bounded implementation, RED-first tests, regression/fault/adversarial tests, static checks, self-review, compact docs and durable evidence |
| GPT-6 Astra / Poppy Javis | Independent R3 scrutiny, ambiguous concurrency/trust/identity problems, hard blockers other agents cannot resolve; exact-source findings and packet suggestions to Sol |
| GPT-5.6 Sol | Architecture decisions, claims/dependencies/scope adjudication, independent-review assignment, exact-SHA acceptance, merge and release |

Self-review is useful but does not satisfy independent review of code you authored. Do not spawn
another model/session to simulate independence unless Sol releases that separate review lane.

## 4. Work deeply inside the existing G0–G10 plan

The complete steps, acceptance matrix and allowed files are in the released packet. Do not
regenerate them. Use this compact nesting to maintain focus:

- Parent goal: WO226 accepted-authority reviewer execution candidate, no duplicated model effect.
- G0/G1: recover -> trace actual C0/GraphDispatch/durable job/supervised execution/resource paths.
- G2/G3: reproduce Astra F1/F2/F3 and prove Sol's proposed canonical winner/resource association
  before building a launch-capable path; then bounded READ_ONLY/pure-plan extension.
- G4: implementation from REDs through the existing ZRA-1 supervised lifecycle.
- G5/G6: race/crash/lost-ack/owner-change/identity-drift and artifact-handoff fault matrix.
- G7/G8: directly related regression/static/secret/scope checks and adversarial self-review.
- G9/G10: freeze one coherent SHA, Draft PR, CI observation, durable handback, external gate.

Use the time/token capacity needed for useful evidence. No artificial hour/test/case quotas;
finish early when acceptance is met. Do not repeat passing suites without changed source,
authority drift, failure or a new discriminating hypothesis. Batch related defects into one
bounded repair cycle; obey the packet/repository repair budget. If the same material defect
persists after that budget, report cause/options/evidence for Astra/Sol instead of blind retries.

Hard case focus: prove or falsify `DurableJobExecutionCoordinator` version-CAS GATING->EXECUTING
as Sol's proposed winner. Keep GraphDispatch job ID, supervised `job:<review_contract_ref>`,
dispatch execution ID and actual runtime execution ID distinct and cross-bound. A successful
CAS at the wrong identity or effect boundary is not proof. Provider EXISTING/lease reentry are
recovery APIs, not launch-winner fences. If existing authorized primitives cannot meet the
contract, stop DESIGN_GAP before effects and give the shortest failing synthetic reproducer.
Do not invent a new lock, store, schema, journal, scheduler or broad scope expansion.

## 5. Durable progress and direct handback

Keep detailed outputs in the selected source lane's ignored `runs/WO-P1-226/`.
After each completed subgoal or material blocker, update a compact checkpoint/notes there with:
current Gx, exact Git identity, owner/claim, changes, command/exit/evidence ref, verified facts,
remaining uncertainty, next safe action and forbidden scope. Preserve evidence already proven.
Before context rollover, freeze this checkpoint; the next context resumes it, not the whole task.
Use existing WO/result protocols; these are evidence files, not a new schema/authority.

Required final result: `runs/WO-P1-226/result.md` in the actual source worktree, with the packet's
full result contract. Do not report only in chat or ask the user to carry result text to GPT.

Before stopping/delegating: update the active WO checkpoint only if that exact file is assigned
to your lane. Global CURRENT-WORK/handoff/COLLAB remain Sol-owned unless explicitly transferred;
supply their fold inputs in your allowed checkpoint and Issue214. Preserve other owners' docs.
Push authorized tracked source/evidence, open/update one Draft PR, and post a concise Issue214
handback with full SHA, Windows result path, tested claims/limits, findings and next gate. Include
sufficient sanitized summary and committed evidence references for reviewers on another host;
never publish raw private logs/credentials. Sol/Astra can read the result destination directly.
If transport to GitHub fails, checkpoint locally with that exact failure; do not label delivered.

Stop at the first actual external CI/review/acceptance gate as the released packet requires;
no polling/sleep loop, no self-merge, no unapproved live provider call. Within an authorized
active subgoal, continue safe work without unnecessary human confirmation.

## 6. Roadmap follow-through (planning, not today's source authority)

| Frontier | Next ownership/release condition |
| --- | --- |
| WO226 reviewer execution | Current released task; complete this candidate and hand back |
| WO223 v2 / C1 semantic evidence | Only after WO226 acceptance/merge/post-main + explicit Sol release; reuse PR305 packet, no new C1 campaign |
| WO205 Phase D | Sol releases after (WO225->WO226->WO223) plus accepted WO208 and WO224 evidence; consume already-proven fold/release effects first |
| WO227 ZRA-3 production continuation | Full ZRA-2 accepted + Issue215 release; reconcile existing ZRA-3 identity/reachability, do not self-accept historical stack |
| ZRA-4 bounded parallel | ZRA-3 accepted + Issue216 release and existing ownership/physical-identity packets; no duplicate preflight or blind parallel writers |
| Later ODP/product completion | Follow Sol's current PROJECT-PLAN/work-order frontier after accelerator acceptance; no whole-project DONE from one lane |

Do not execute the next row from this invocation after an external gate. On a later authorized
invocation, re-pin Sol's current NEXT_READY and use its existing packet. Astra/Sol update durable
pointers/decisions; the human need not relay findings back. This entry cannot wake a stopped ZCode
session: automatic resumption is a product acceptance requirement, not a capability created by prose.

Zero-Relay is achieved only when Sol accepts evidence that the production path performs eligible
packet dispatch -> exact result ingestion -> verify/review -> bounded repair -> NEXT_READY
continuation, with required ZRA-4 isolation and no human prompt/result transfer between those
steps. Record relay counts and production reachability; do not infer completion from definitions,
unit tests, a long GLM session, this one-pointer fallback, or a synthetic-only happy path.
